/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false, // You can set this to true if you want to enable Strict Mode
  output: 'export', // This tells Next.js to export a static site
  // Other configurations you might need
  // You can specify other options here if necessary
};

module.exports = nextConfig;
